<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	http://www.springframework.org/schema/security
	http://www.springframework.org/schema/security/spring-security.xsd">


    <http auto-config="true">
        <intercept-url pattern="/rest/*" access="ROLE_ANONYMOUS, ROLE_USER, ROLE_ADMIN"/>
        <intercept-url pattern="/rest/admin/*" access="ROLE_ADMIN"/>
        <intercept-url pattern="/rest/write/*" access="ROLE_USER, ROLE_ADMIN"/>

        <intercept-url pattern="/ii" access="ROLE_USER,ROLE_ADMIN"/>

        <form-login login-page="/login"
                    default-target-url="/ii"
                    authentication-failure-url="/loginError"/>


        <logout logout-url="/logout" logout-success-url="/login"/>
        <logout/>
        <anonymous username="guest" granted-authority="ROLE_ANONYMOUS"/>
        <remember-me/>
    </http>


    <authentication-manager>
        <authentication-provider>
            <jdbc-user-service data-source-ref="dataSource"
                               users-by-username-query=
                                       "select username, password, enabled from users where username=?"
                               authorities-by-username-query=
                                       "SELECT username, role
                                       FROM user_role, roles, users
                                       WHERE
                                        users.username=? AND
                                        user_role.id_role=roles.role_id AND
                                        user_role.id_user=users.id  "/>
        </authentication-provider>
    </authentication-manager>


</beans:beans>